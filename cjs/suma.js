function suma (a, b) {
  return a + b;
}
// CommonJS Module Export
module.exports = {
    suma
} // Exportamos la funcion suma para que pueda ser utilizada en otros archivos
// Esto permite que otros archivos puedan importar y utilizar la función suma